{
  "name": "draftport-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "npm --prefix apps/tauri run dev",
    "dev:web": "pnpm --filter @draftport/web run dev",
    "dev:tauri": "npm --prefix apps/tauri run dev",
    "dev:desktop": "npm --prefix apps/tauri run dev",
    "build:desktop": "npm --prefix apps/tauri run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.17.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.16",
    "globals": "^16.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.0.0",
    "turbo": "^2.7.2",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.50.0",
    "wait-on": "^7.0.1"
  },
  "lint-staged": {
    "apps/web/**/*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "packages/core/**/*.{ts,js}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,css}": [
      "prettier --write"
    ]
  }
}
