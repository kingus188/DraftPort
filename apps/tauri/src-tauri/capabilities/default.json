{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the main DraftPort window to call app-owned desktop commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
